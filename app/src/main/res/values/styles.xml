<resources>

    <!-- 🔹 Style for Navigation Drawer Text -->
    <style name="NavDrawerTextStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- 🔹 Style for Calendar Weekday Headers -->
    <style name="CalendarDayHeader">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/hintGray</item>
    </style>

    <!-- 🔹 Custom style for Mood Emojis -->
    <style name="MoodEmojiText">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">42sp</item>
        <item name="android:padding">8dp</item>
    </style>

    <!-- 🔹 Mood Day (Calendar Cell) -->
    <style name="MoodDayCell">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">64dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_calendar_day</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/darkGray</item>
    </style>

</resources>
