<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- White background -->
    <solid android:color="@color/white" />

    <!-- Rounded corners -->
    <corners android:radius="25dp" />

    <!-- Thin border -->
    <stroke android:width="1dp" android:color="@color/cream" />
</shape>
